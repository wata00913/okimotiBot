<!DOCTYPE html>
<html>
  <head>
    <title>OkimotiBot</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <header class="p-4 bg-gray-100 flex flex-col fixed top-0 w-full h-28">
      <div class="flex justify-between w-full mb-4">
        <%= link_to 'okimotiBot', messages_path,  class: 'text-5xl text-blue-500' %>

        <% if user_signed_in? %>
          <%= link_to 'ログアウト', destroy_user_session_path, data: { turbo_method: :delete }, class: 'px-4 py-2 bg-blue-500 text-white rounded-full hover:bg-blue-600 focus:outline-none focus:ring focus:ring-blue-300' %>
        <% end %>
      </div>

      <% if user_signed_in? %>
        <%= link_to 'ユーザー情報編集', edit_user_registration_path, class: 'text-blue-500 hover:underline' %>
      <% end %>
    </header>

    <main class="mt-28">
      <% if notice %>
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative" role="alert">
          <span class="block sm:inline"> <%= notice %> </span>
        </div>
      <% end %>

      <% if alert %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative" role="alert">
          <span class="block sm:inline"> <%= alert %> </span>
        </div>
      <% end %>

      <div class="max-w-xl mx-auto my-8">
        <%= yield %>
      </div>
    </main>
  </body>
</html>
