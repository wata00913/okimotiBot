export class ObservedChannelMembers {
  constructor() {
    this.data = []
  }

  // 監視ユーザーのON, OFF
  toggleObserve(channelId, accountId) {
  }

  // チャンネルの追加
  registerChannel(channelId, accountIds) {
  }

  // チャンネルの削除
  deleteChannel(channel_id) {
  }
}
